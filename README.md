# War Counter ready files

Copy the `app` folder from this zip into your existing `war-counter` project.

What it contains:
- app/layout.tsx
- app/page.tsx
- app/globals.css

After copying, refresh http://localhost:3000.
If the server does not update, stop it with Ctrl+C and run `npm run dev` again.
